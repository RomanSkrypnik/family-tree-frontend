import React, { FC } from 'react';
import { BranchDto } from '../ts';

interface Props {
    branch: BranchDto;
}

export const BranchList: FC<Props> = ({ branch }) => {



    return (
        <div>

        </div>
    );
};
